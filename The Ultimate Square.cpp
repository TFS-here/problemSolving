#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    int t;
    cin>>t;
    while(t--)
    {
        double n;
        cin>>n;
        double ans=(n/2);
        //n/2==0?ans=ans:ans=ans+1;
        cout<<ans<<endl;

    }
    return 0;
}