#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int w,l,ans;
    while(true)
    {
        cin>>w>>l;
        //int m=sqrt(l/2+l/2);
        cout<<w<<l<<endl;
        if(w==0&&l==0)
            break;
    }
}
